import { Test, TestingModule } from '@nestjs/testing';

describe('Lots Controller', () => {

   // todo
    it('lots query returns array', () => {
      const lots = [];
      expect(typeof lots).toBe('object');
    });
  
    it('lot by id', () => {
      const lots = [];
      expect(typeof lots).toBe('object');
    });
  
    it('lot by wrong id', () => {
      const lots = [];
      expect(typeof lots).toBe('object');
    });
  
});
